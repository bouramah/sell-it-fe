# En dev, pointe vers l'API locale. En prod, laisser vide — nginx sert le build et proxy
# /api + /uploads vers le backend sur la même origine (chemins relatifs).
VITE_SERVER_BASE=http://localhost:8000
